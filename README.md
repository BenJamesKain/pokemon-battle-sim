# pokemon-battle-sim
Pokemon battle simulator written in Java. Created for CS 330 Prog. Lang.

The simulator is written in a custom programming language, which uses the double dispatch pattern.
Includes a Tokenizer, Parser, Visitors, and a driver.
